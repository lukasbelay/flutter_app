import 'package:flutter/material.dart';

class Sus extends StatelessWidget {
  const Sus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text("this is sus")),
      );
}
